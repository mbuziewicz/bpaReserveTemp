@page
@model BpaReserve.Pages.RideReserve.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RideReservation[0].DateAndTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RideReservation[0].user)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RideReservation[0].Ride)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.RideReservation) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateAndTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.user.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ride.RideID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
